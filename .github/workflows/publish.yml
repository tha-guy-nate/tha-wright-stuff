name: Publish

on:
  push:
    tags:
      - "v*"

jobs:
  publish:
    permissions:
      id-token: write
      contents: write
    uses: tha-guy-nate/tha-github-workflows/.github/workflows/python-publish.yml@main
    secrets: inherit
